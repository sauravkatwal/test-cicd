export enum WorkspaceResourceTypeEnum {
    sqs = "sqs",
    email = "email",
    viber = "viber",
    sms = "sms",
    whatsapp = "whatsapp",
}

export enum WorkspaceResourcePurposeEnum {
    campaign = "campaign",
    import = "import",
}
