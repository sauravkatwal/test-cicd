export enum IdentityType {
  EmailAddress = 'EmailAddress',
  Domain = 'Domain',
}

export enum AwsSesStatus {
  success = "success",
  pending = "pending",
  failed = "failed",
  temporaryFailure = "temporary_failure",
  notStarted = "not_started",
}
