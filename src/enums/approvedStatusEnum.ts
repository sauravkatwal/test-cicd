export enum CampaignApprovedStatus {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected', 
}

export enum EmailTemplateApprovedStatus {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected', 
}